# 使用法
1. 身長に変更がある場合のみ、`consts_files`の`my_weight`の値をメートル表記で設定する。変更がない場合は変更しない。
2. `script.ipynb`を `runAll`で実行する。
3. 1セル目を実行した際に体重を整数で入力する。なお、整数以外の場合は入力を受け付けない。
4. 1セル目の実行結果として現在の体重とbmiが表示される

【備考】
- 1は現在は標準入力方式に一時的に変更中のため仕様と異なる動作をする 
